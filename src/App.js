import React from 'react';
import { Typography } from '@material-ui/core';

export default function App(){
    return (
        <Typography variant="h1">This is React app setup with Parcel Bundler</Typography>
    )
}